======================================================================
PROGRESS REPORT — DIGITAL PRODUCTS STOREFRONT & E-WALLET SYSTEM
======================================================================

[BERHASIL] — Completed & Verified
----------------------------------------------------------------------
Backend (/server) — ALL DONE, endpoints tested via curl:
- index.js ............ Express entry, CORS + JSON middleware, auto-seed
- database.js ......... SQLite (better-sqlite3) schema: users, categories,
                        products, items_stock, deposits, transactions
- seed.js ............. Auto-seeds admin/user, 3 categories, 5 products,
                        15 stock items (idempotent — skips if data exists)
- middleware/auth.js .. JWT verify (requireAuth) + role guard (requireAdmin)
- routes/auth.js ...... POST /register, POST /login, GET /me
- routes/products.js .. GET / (with live stock count), GET /:id
- routes/wallet.js .... POST /deposit, POST /deposit/confirm (atomic tx),
                        GET /history (deposits + transactions)
- routes/buy.js ....... POST / — atomic SQLite transaction: balance check,
                        stock allocation, balance deduction, item marked
                        sold, transaction logged, instant delivery
- routes/admin.js ..... POST /products, POST /stock/bulk,
                        GET /deposits/pending, POST /deposits/approve

Verified end-to-end (curl):
  login -> deposit (DANA 25k) -> confirm (balance 50k -> 75k)
  -> buy Netflix (delivered netflix01@mail.com:Pass1234, balance 30k)
  -> buy ML topup with target_data (AUTO_FULFILLED, balance 10k)
  -> insufficient balance correctly rejected

Frontend (/client) — ALL DONE, production build passes:
- Vite + React 18 + Tailwind CSS v4 + lucide-react, /api proxy -> :5000
- context/AuthContext.jsx .. global session + live balance + toast system
- utils/api.js ............. fetch helper w/ JWT header, formatRupiah
- components/Navbar.jsx .... logo, balance pill + quick "+" topup, dropdown
- components/ProductCard.jsx  icon, price, stock badge (green/red), Beli
- components/DepositModal.jsx  quick amounts (10/25/50/100k) + custom,
                        4 payment methods, simulated QR/payment code,
                        "Simulasi Bayar / Confirm" button
- components/Toast.jsx ..... success/error/info notifications
- pages/Home.jsx ........... hero banner, category filter bar, product grid
- pages/ProductDetail.jsx .. order form, game_topup ID input, balance vs
                        price summary, insufficient warning + Top Up
                        button, instant success modal w/ COPY TO CLIPBOARD
- pages/Wallet.jsx ......... balance card + deposit modal
- pages/History.jsx ........ Tab "Pembelian Saya" + Tab "Mutasi Saldo"
- pages/AdminDashboard.jsx . add product form, bulk stock textarea,
                        pending deposit approval table w/ 1-click approve
- pages/Login.jsx / Register.jsx  w/ demo account hint box

Root:
- package.json (concurrently: npm start runs both), .gitignore
- Dependencies installed for root, server, and client.

[PENDING] — Not yet done / could be improved
----------------------------------------------------------------------
- No automated test suite (only manual curl + production build checks).
- No countdown timer on the deposit invoice card (plan item 4D).
- Hero banner is static — no promo carousel.
- No pagination/search on catalog.
- Deposit "expired" auto-cancel not implemented.
- No rate limiting / helmet security hardening.
- Admin category list is hardcoded (ids 1-3) instead of fetched from API.

[NEXT STEPS] — For the next developer/agent
----------------------------------------------------------------------
1. Run: npm run install-all  (if fresh clone), then: npm start
   -> server on http://localhost:5000, client on http://localhost:3000
2. Delete server/store.db (and -wal/-shm) to re-seed from scratch.
3. Add countdown timer to DepositModal invoice step (step === 2).
4. Replace hardcoded categories in AdminDashboard with a GET /api/categories
   endpoint (add it to routes/products.js).
5. Consider adding express-rate-limit and helmet before any real deployment.
6. Optional: write integration tests for /api/buy concurrency (double-buy
   of last stock item) to prove the atomic transaction holds.
